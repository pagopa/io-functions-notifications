name: Deploy Pipelines - PROD

on:
  workflow_dispatch:
  push:
    branches:
      - CES-51-devops-decommissioning
      
jobs:
  deploy_on_prod:
    uses: pagopa/dx/.github/workflows/legacy_deploy_pipelines.yaml@workflow-functions-test
    name: Deploy on PROD
    secrets: inherit
    with:
      environment: 'prod'
      resource_group_name: 'io-p-messages-weu-prod01-push-notif-rg'
      app_name: 'io-p-push-notif-fn'
      use_staging_slot: true
      use_private_agent: false
      health_check_path: '/api/v1/info'